@doc """
A simple and contrived dynamic list implementation in Elixir
"""


defmodule Array do

  def new() do
    :array.new(1)
  end

  def append(array, item) do
    :array.set()
  end

  defp get_first_unset_index(array) do
    
  end
end
